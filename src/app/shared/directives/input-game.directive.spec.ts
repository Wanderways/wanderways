import { InputGameDirective } from './input-game.directive';

describe('InputGameDirective', () => {
  it('should create an instance', () => {
    const directive = new InputGameDirective();
    expect(directive).toBeTruthy();
  });
});
